<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/act_splash"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        layout="@layout/lay_viewpager_textview"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    <!--<android.support.v4.view.ViewPager
        android:id="@+id/sysViewPagerIntroduction"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:listitem="@layout/pager_item" />

    <RelativeLayout
        android:id="@+id/viewPagerIndicator"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_alignParentBottom="true"
        android:background="#73000000"
        android:gravity="center"
        android:padding="4dp">

        <com.rz.varnishview.introviewpager.ViewPagerIndicator
            android:id="@+id/sysViewPagerIndicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_gravity="center_horizontal|bottom"
            android:layout_marginBottom="20dp"
            app:delimiterSize="0dp"
            app:itemIcon="@drawable/white_circle"
            app:itemScale="1.9"
            app:itemSelectedTint="@color/colorAccent"
            app:itemSize="6dp"
            app:itemTint="@color/colorPrimary" />
    </RelativeLayout>-->
</RelativeLayout>
